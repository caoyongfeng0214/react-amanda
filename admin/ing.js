import React from "react";

import LinearProgress from '@mui/material/LinearProgress';

var Ing = function Ing(props) {
    return React.createElement(LinearProgress, { sx: { width: '100%' } });
};

export default Ing;